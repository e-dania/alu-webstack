#!/usr/bin/env bash

domain=$1

for subdomain in "www" "lb-01" "web-01" "web-02"; do
    ip=$(dig +short ${subdomain}.${domain})
    if [ -n "$ip" ]; then
        echo "The subdomain ${subdomain}.${domain} points to $ip"
    else
        echo "Failed to resolve ${subdomain}.${domain}"
    fi
done
